function Broadcast(list, BitMEXData) {
    this.list = list;
    this.BitMEXData = BitMEXData;
}

Broadcast.prototype.rekt = function () {
    let list = this.list;
    let BitMEXData = this.BitMEXData;

    // 未完成
    // list.map((ele, idx) => {
    //     if (ele.brodcast.rekt == true) {

    //     }
    // })

    // let latestRecord;
    // while (latestRecord = BitMEXData.rekt.shift()) {
    //     bot.push(ele.id, latestRecord.message)
    //     latestRecord = null;
    // }
}

module.exports = Broadcast;